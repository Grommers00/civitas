#Front end

# IP is the IP used by the web server default for local host is 10.0.2.2 if you running an android VMachine
IP=10.0.2.2

# PORT is the PORT used by the web server this is the same as the backend
PORT=:3000

